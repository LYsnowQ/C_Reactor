#pragma once
#include "Channel.h"


struct Dispathcer
{
    //init select/pool/epoll's datablock
    void* (*init)();    
    int (*add)(struct Channel* channel);
    int (*remove)();
    int (*modify)();
    int (*dispatch)();
    int (*clear)();
};
